# Add-on runs in Alpine Linux — force LF so Docker build + bashio shebang don't choke on CRLF.
* text=auto eol=lf
*.sh    text eol=lf
Dockerfile text eol=lf
*.py    text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
